# car-doctor Server

## Setup

```sh
npm i mongoose
npm i zod dotenv
npm i jose  # better than jsonwebtoken
npm i -D typescript @types/node @types/express

```

## Test

```sh
pnpm i -d vitest
```

Modify vitest.config.ts, tsconfig.json
